<section class="py-10">
  <div class="max-w-5xl mx-auto px-4">
    <div class="flex flex-col gap-4 md:flex-row md:items-end md:justify-between">
      <div>
        <p class="text-xs uppercase tracking-[0.25em] text-blue-300">Activite de mes services</p>
        <h1 class="mt-2 text-3xl font-semibold tracking-tight">Paiements recus</h1>
        <p class="mt-2 text-slate-400">
          Retrouvez les paiements deja enregistres sur vos services.
        </p>
      </div>
      <a href="/users/profile" class="text-sm text-blue-300 hover:text-blue-200 transition">
        Retour au profil
      </a>
    </div>

    <div
      class="payment-summary-card service-card--interactive mt-8"
      tabindex="0"
      data-card-focus="service-payments-list"
    >
      <p class="payment-summary-card__label">Total encaisse</p>
      <p class="payment-summary-card__value"><%= totalRevenue.toString().replace(".", ",") %> EUR</p>
      <p class="payment-summary-card__copy">Montant total deja recu sur vos services payes.</p>
    </div>

    <div id="service-payments-list" class="mt-8 space-y-4">
      <% if (!payments.length) { %>
        <div class="rounded-2xl border border-white/10 bg-white/5 px-5 py-6 text-slate-400">
          Aucun paiement recu pour le moment.
        </div>
      <% } else { %>
        <% payments.forEach((payment) => { %>
          <a href="/users/profile/service-payments/<%= payment.id %>" class="block rounded-2xl border border-white/10 bg-white/5 px-5 py-5 transition hover:border-white/20">
            <div class="flex flex-wrap items-center justify-between gap-4">
              <div>
                <h2 class="text-lg font-medium text-white"><%= payment.service_title %></h2>
                <p class="mt-1 text-sm text-slate-400">
                  Regle par : <span class="text-slate-200"><%= payment.client_name %></span>
                </p>
                <p class="mt-1 text-sm text-slate-500">
                  Mode : <span class="text-slate-300 capitalize"><%= payment.payment_method || "Paiement" %></span>
                </p>
              </div>
              <div class="text-right">
                <p class="text-emerald-300 font-medium"><%= (payment.amount ?? 0).toString().replace(".", ",") %> EUR</p>
                <p class="mt-2 text-xs text-slate-500">
                  <%= payment.payment_date ? new Date(payment.payment_date).toLocaleDateString("fr-FR") : "" %>
                </p>
              </div>
            </div>
          </a>
        <% }) %>
      <% } %>
    </div>
  </div>
</section>
